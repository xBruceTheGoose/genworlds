{
  "name": "genworlds",
  "version": "0.1.0",
  "private": true,
  "description": "GenWorlds - Collaborative AI Agent Framework (Python project)",
  "scripts": {
    "build": "mkdir -p dist && cp -r build/* dist/"
  },
  "engines": {
    "node": ">=16.14"
  }
}
