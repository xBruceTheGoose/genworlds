{
  "name": "genworlds",
  "version": "0.1.0",
  "private": true,
  "description": "GenWorlds - Collaborative AI Agent Framework (Python package with Docusaurus docs)",
  "scripts": {
    "docs:install": "cd docs && npm install",
    "docs": "cd docs && npm start",
    "docs:build": "cd docs && npm run build",
    "docs:serve": "cd docs && npm run serve",
    "build": "echo 'This is a Python project. Use poetry install instead. For docs, run npm run docs:build'"
  },
  "engines": {
    "node": ">=16.14"
  }
}
